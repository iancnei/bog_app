<% @creatures.each do |creature| %>
	<div>
		<h2> Name: <%= creature.name %><br> </h2> 
		<p>Description: <%= creature.description %><br></p>
		<%= button_to "Delete", creature, method: :delete %>
	</div>
<% end %>